# 0402 运动侧速度识别 program

## 任务定义
- 当前任务只看 `20240402` 的运动学数据。
- 先不碰 EEG。
- 当前目标是判断文件名里带出的速度档，能不能被稳定映射到粗速度标签。

## 当前标签模式
- `coarse_speed_v1`
- 速度档映射固定为：
  - `12 / 15 -> slow`
  - `18 / 21 -> medium`
  - `24 / 24_30 -> fast`

## 当前可执行边界
- 只允许扫描文件名，不读取 xlsx 内容。
- 只允许做 motion-only 的最小脚手架。
- 不把这个任务和步态支撑 / 摆动主线混在一起。

## 第一版 4 个 track
- `ridge`
- `tree_xgboost`
- `feature_gru`
- `feature_tcn`

## 当前交付目标
- 先产出能被陈老师和内部复现的 4-track manifest。
- 再用同一份文件名速度档，确认粗速度桶是否稳定。
