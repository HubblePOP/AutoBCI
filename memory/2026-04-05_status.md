# 2026-04-05 现状记录

## 当前 accepted best

- 当前 accepted best：`stageC_ridge`
- 当前主线数据集：`walk_matched_v1_64clean_joints`
- 当前目标：第二个 sheet 的 `8` 个关节角
- 当前训练脚本：`scripts/train_ridge.py`
- 当前预处理：`car_notch_bandpass`
- 当前特征：`lmp + hg_power`，`100 ms` 分箱后取 `mean`
- 当前正式结果：
  - `val mean_pearson_r_zero_lag_macro = 0.3180`
  - `test mean_pearson_r_zero_lag_macro = 0.2322`
  - `test mean_mae_deg_macro = 9.3294`
  - `test mean_rmse_deg_macro = 11.8382`

## 当前主线怎么跑

- split：`18 train / 2 val / 2 test`
- 脑电输入：每条 session 只保留有效半区的 `64` 通道
- 时间设置：
  - `window_seconds = 3.0`
  - `stride_samples = 400`
  - `feature_bin_ms = 100.0`
- formal 主指标：`val mean_pearson_r_zero_lag_macro`
- 每条 formal 结果都保留：
  - 每关节 `r`
  - 每关节 `MAE`
  - 每关节 `RMSE`
  - 每关节 `gain`
  - 每关节 `bias`
  - 压幅排行榜

## 当前问题队列

### Phase A

- `mean` 伪迹排查已经做完
- 结论：`mean` 不是纯 session 常数偏置，但也还不能当成干净神经证据

### Phase B

- `lmp`
- `hg_power`
- `lmp + hg_power`
- `bandpower_bank`

当前最好的是 `lmp + hg_power + ridge`

### Phase C

- 当前比较对象：
  - `ridge`
  - `RF`
  - `XGBoost`
  - `small feature-LSTM`
- 当前 accepted best 先固定为 `stageC_ridge`
- `feature-LSTM` promotion packet 已完成
  - `seed0/1/2` 全部跑完
  - gate：`pass`
  - 中位数 `val mean_pearson_r_zero_lag_macro = 0.4227`
  - 中位数 `test mean_pearson_r_zero_lag_macro = 0.3483`
- 当前更强的单次候选是 `stageC_xgboost_256`
  - `val mean_pearson_r_zero_lag_macro = 0.4329`
  - `test mean_pearson_r_zero_lag_macro = 0.3748`
- 按当前规则，因为 `XGBoost` 还没有复验，所以 accepted best 继续保持 `stageC_ridge`
- 当前片段级对照保留：
  - `ridge`
  - `RF`
  - `feature-LSTM`
- `XGBoost` 的 formal 和压幅指标保留，但 checkpoint 还不能稳定重载做片段级预测

### Phase D

- `within_session_upper_bound` 继续保留
- 单独记账
- 不更新主线 accepted best
- 当前 `upper_bound feature-LSTM`：
  - `stageD_upper_bound_lmp_hg_feature_lstm`
  - `val mean_pearson_r_zero_lag_macro = 0.4394`
  - `test mean_pearson_r_zero_lag_macro = 0.4745`
  - 这条结果是从当前 best checkpoint 直接做 formal eval 得到的

## 当前门禁和记账

- `bank-QC` 现在是 campaign 的正式门禁
- 当前 monitor 顶部显示：
  - `accepted best`
  - `feature_family`
  - `model_family`
  - `evaluation_mode`
- ledger 现在分成两条：
  - 主线 `experiment_ledger.jsonl`
  - 上限线 `upper_bound_ledger.jsonl`

## 关键实验轨迹

1. `raw128_control`
   - 作用：证明链路能跑

2. `64clean_raw_lstm`
   - 结论：只做 bank 清理还不够，raw 时域 LSTM 表现很弱

3. `joints_sheet_baseline_000`
   - 结论：joints 目标接通了，但 LSTM 基线只到 `test r = 0.0417`

4. `joints-campaign-002-iter-001`
   - 结论：`mean + abs_mean + rms + ridge` 把主线推到 `test r = 0.1828`

5. `stageC_ridge`
   - 结论：`car_notch_bandpass + lmp + hg_power + ridge` 现在是当前主线 baseline

6. `stageC_feature_lstm_seed_sweep`
   - 结论：`feature-LSTM` 在固定 `lmp + hg_power` 下稳定优于 `ridge`

7. `stageC_xgboost_256`
   - 结论：当前给出了更强的单次 formal 结果，但还不能直接升主线

## 当前限制

- 当前 `test` 已经被看过很多次，只能当开发参考
- 当前主线最明显的问题还是跨 session 偏移和压幅
- `Mcp / Elb / Kne` 这类关节还需要继续看 `gain << 1` 的情况

## 代码仓库

- 新建 GitHub 仓库：`infoechoes/AutoBci`
- 本地路径：`/Users/mac/Code/AutoBci`
- 远端：`https://github.com/infoechoes/AutoBci.git`
- 当前本机状态：
  - `gh` 已安装
  - 当前登录账号：`infoechoes`
  - 仓库已经完成 clone，可直接作为实验项目代码仓库使用
